import 'package:flutter/material.dart';

class Folder extends StatelessWidget {
  const Folder({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Text("Folder",style: TextStyle(fontSize: 100),),
    );
  }
}
